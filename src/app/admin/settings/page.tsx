export default function AdminSettingsPage() {
  return (
    <section>
      <h1 className="font-display text-5xl uppercase">Settings</h1>
      <p className="mt-3 max-w-2xl text-sm text-zinc-700">
        Módulo preparado para editar datos de marca, redes, teléfonos y bloques de copy de home
        desde `siteSettings`.
      </p>
    </section>
  );
}
